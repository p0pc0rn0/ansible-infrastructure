server {
    listen {{ nginx_port | default(80) }};
    server_name {{ nginx_app_server_name | default("app.local") }};

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
