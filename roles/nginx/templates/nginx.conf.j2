user www-data;
worker_processes auto;

events {
    worker_connections 1024;
}

http {
    sendfile on;
    server {
        listen 80;
        server_name {{ nginx_server_name | default("localhost") }};

        location / {
            root /var/www/html;
            index index.html;
        }
    }
}